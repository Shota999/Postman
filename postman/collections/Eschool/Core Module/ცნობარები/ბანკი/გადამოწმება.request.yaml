$kind: http-request
<<<<<<< HEAD
=======
name: გადამოწმება
>>>>>>> 22bc95358f4816c7b6a4e4bc0297199f25486611
url: "{{urlCore}}libraries/bank/napr?code=205232238"
method: GET
queryParams:
  code: "205232238"
scripts:
  - type: afterResponse
    code: |-
      pm.test("სტატუს-კოდი 200", () => {
          pm.response.to.have.status(200);
      });

      pm.test("პასუხის დრო < 2000ms", () => {
          pm.expect(pm.response.responseTime).to.be.below(2000);
      });

      let response = pm.response.json();

      pm.test("პასუხი შეიცავს bankName-ს", () => {
          pm.expect(response).to.have.property("nameEn").that.is.not.empty;
      });

      pm.test("პასუხი შეიცავს bankCode-ს", () => {
          pm.expect(response).to.have.property("code").that.is.not.empty;
      });

      pm.environment.set("bankName", response.name);
      pm.environment.set("bankCode", response.code);
    language: text/javascript
order: 3000
