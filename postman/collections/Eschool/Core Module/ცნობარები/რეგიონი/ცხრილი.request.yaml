$kind: http-request
url: "{{urlCore}}libraries/region?orderBy=id:asc"
method: GET
queryParams:
  - key: page
    value: "1"
    disabled: true
  - key: size
    value: "25"
    disabled: true
  - key: orderBy
    value: id:asc
  - key: search
    value: kakheti
    disabled: true
scripts:
  - type: afterResponse
    code: "let response = pm.response.json();\r

      \r

      pm.test(\"Check if the totalElements is correct\", function () {\r

      \    pm.expect(response.totalElements).to.equal(19);\r

      });\r

      \r

      pm.test(\"Check if the first item's name is correct\", function () {\r

      \    pm.expect(response.content[0].name).to.equal(\"კახეთი1\");\r

      });\r

      \r

      // ვპოულობთ POST-ით შექმნილ ჩანაწერს name-ით (search={{regionName}} ჩართული უნდა იყოს Params-ში)\r

      // const createdName = pm.collectionVariables.get(\"regionName\");\r

      // const created = (response && response.content) ? response.content.find(x => x.name === createdName) : null;\r

      \r

      // ეს ტესტები მხოლოდ მაშინ გაეშვება, თუ ჯაჭვში ვართ (regionName დაყენებულია)\r

      // if (createdName) {\r

      //     pm.test(\"ახლად შექმნილი ჩანაწერი მოიძებნა ცხრილში\", () => {\r

      //         pm.expect(created, \"ვერ მოიძებნა ჩანაწერი სახელით: \" + createdName).to.not.be.undefined;\r

      //     });\r

      \r

      //     if (created) {\r

      //         pm.collectionVariables.set(\"itemLibraryId\", created.id);\r

      //         pm.collectionVariables.set(\"lastUpdateDate\", created.lastUpdateDate);\r

      //     }\r

      // }\r

      \r

      // სტატუსის შემოწმება — მხოლოდ PATCH-ის შემდეგ, როცა expectedStatus დაყენებულია\r

      // const expectedStatus = pm.collectionVariables.get(\"expectedStatus\");\r

      // if (created && expectedStatus) {\r

      //     pm.test(\"სტატუსი გახდა \" + expectedStatus, () => {\r

      //         pm.expect(created.status).to.eql(expectedStatus);\r

      //     });\r

      // }"
    language: text/javascript
order: 2000
